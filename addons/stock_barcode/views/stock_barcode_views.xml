<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Add a button to open the barcode scanner action -->
        <record id="view_stock_picking_form_inherited" model="ir.ui.view">
            <field name="name">stock.picking.form.inherited</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header/button[@name='action_assign']" position="after">
                    <button name="action_open_barcode_scanner" string="Scan Barcode" type="object" class="oe_highlight"/>
                </xpath>
            </field>
        </record>

        <!-- Define the form view for the barcode scanner -->
        <record id="view_stock_barcode_scanner_form" model="ir.ui.view">
            <field name="name">stock.barcode.scanner.form</field>
            <field name="model">stock.barcode.scanner</field>
            <field name="arch" type="xml">
                <form string="Barcode Scanner">
                    <header>
                        <button string="Scan" type="object" name="action_scan_barcode" class="oe_highlight"/>
                        <button string="Cancel" type="object" name="action_cancel" class="oe_link"/>
                    </header>
                    <sheet>
                        <group>
                            <field name="barcode"/>
                            <field name="picking_id" invisible="1"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>
